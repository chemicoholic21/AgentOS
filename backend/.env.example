DATABASE_URL=postgresql://daytona:daytona@localhost:5432/agentos
# Set your key to use the live Claude API. Without it, agents run in MOCK mode
# (deterministic offline responses) so the full loop is still demoable.
# ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-20250514
